version: "3.1"
intents:
  - greet
entities:
  - frendo
  - project_name
responses:
  utter_greet:
    - text: hi how are you
    - text: mulibwanji
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
